# указываем образ, с которого берем всю инфу по версии 3.8
FROM python:3.8 
# создается рабочая директория с файлами, которые необходимо будет выполнить при помощи данного образа 
WORKDIR /app
# копируем файл app_1.py (. - елси все фыйлы из этой директории) в папку app, которая создается непосредственно в образе
COPY app_1.py /app
# единоразове дейсвтие при создании образа  
RUN echo "flask" > req.txt

RUN pip install -r req.txt
# набор  действий, которые будут выполнятся каждый раз при запуске контейенера 
CMD ["python", "app_1.py"]

