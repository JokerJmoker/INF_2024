# указываем образ, с которого берем всю инфу по версии 3.8
FROM python:3.8
# создается рабочая директория с файлами, которые необходимо будет выполнить при помощи данного образа 
WORKDIR /app
# тепреь уже копируем все файлы в рабочую папку
COPY . /app
# вариант реализации без создания фацла с зависимостями 
RUN echo "\
flask\n\
pymongo\n\
" > req.txt
# запускаем установки необходимого окружения и т.д
RUN pip install -r req.txt

CMD sleep 5 && python create_db.py && python app.py